allprojects {
    afterEvaluate { p ->
        if (p.path == ':app') {
            def cfg = p.configurations.findByName('releaseCompileClasspath')
            if (cfg != null) {
                cfg.resolve().each { println it.absolutePath }
            }
        }
    }
}
